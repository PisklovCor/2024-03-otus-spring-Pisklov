## Приложение с помощью MongoDB

Приложение предоставляет CRUD-операции с 4-мя объектами (Книги, Аторы, Жанры и Комментарии)

#### Сборка проекта:
```
mvn clean package
```

#### Запуск MongoDb в docker:
```shell
docker-compose up -d
```